# Pandas is a module and helpful for Data Analysis

# To install pandas - pip install pandas 

---------------------------------------------------------------------------
-- Generic Points --

-- date_range() - It returns a series of date values from a given 'start' till
   'end' value. We can pass other arguments like 'freq', 'periods', 'tz' which is 
   timezone, 'units' etc. 




---------------------------------------------------------------------------